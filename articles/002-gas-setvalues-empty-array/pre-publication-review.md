# 公開前レビュー

## 対象

- 記事ID: 002
- 記事タイトル: GASでデータが0件のときにsetValues()がエラーになる原因と対処方法
- フォルダ: `articles/002-gas-setvalues-empty-array/`

## AIレビュー結果

公開前の内容確認を実施しました。

- [x] 会社名・顧客名が残っていない
- [x] 商品名・商品IDが残っていない
- [x] 実案件の画像URLが残っていない
- [x] APIキー・トークン・認証情報が含まれていない
- [x] 社内URL・非公開URLが含まれていない
- [x] 実務固有のシート名・変数名を公開用に一般化している
- [x] `setValues()` がRangeと同じサイズの2次元配列を必要とすることをGoogle公式情報で確認した
- [x] 0件時は `values[0]` が `undefined` となるため、その参照前に件数判定する構成になっている
- [x] 0件を正常ケースとして扱う方法と、後続処理を継続する方法を区別している
- [x] CodePenが不要なGAS記事であることを確認した
- [x] 「動作確認済み」と誤認される記載をしていない

## 人間による確認が必要な項目

- [ ] Google Apps Script実環境で `sample.gs` を実行し、データ1件以上で書き込みできることを確認する
- [ ] `buildSampleRows_()` を `return [];` に変更し、0件でもエラーにならず終了することを確認する
- [ ] WordPress下書き反映後にコードブロック・見出し・リンク表示を確認する

## 判定

AIによる公開前レビュー: **合格**

ただし、Google Apps Script実環境での人間による動作確認は未実施です。
